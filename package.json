{
  "name": "movies",
  "verison": "1.0.0",
  "engines": {
    "node": ">=18 <19"
  },
  "scripts": {
    "start": "cd server && npm start",
    "prebuild": "cd server && npm run build",
    "build": "cd client && npm run build"
  }
}

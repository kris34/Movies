import { TitleShortenPipe } from './title-shorten.pipe';

describe('TitleShortenPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleShortenPipe();
    expect(pipe).toBeTruthy();
  });
});
